-- ============================================================================
-- V1 - Initial Schema Setup
-- ============================================================================
-- This migration sets up the base database schema for the School Library application
-- Executed automatically by Flyway on application startup

-- Currently placeholder - no tables created yet as we're in "Hello World" phase
-- Each bounded context module will add its own schema in future migrations

-- Status: EXECUTED
